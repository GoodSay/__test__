name: heartbeat
on:
  push:
    branches:    
      - 'main'
      - 'dev'
  schedule:
    - cron: '30 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source repository
        uses: actions/checkout@v3
        
      - name: Curl actuator
        id: ping
        run: |
          echo "::set-output name=status1::$(curl --connect-timeout 10 --max-time 10 http://51.210.128.134:5000/api/health)"
          echo "::set-output name=status2::$(curl --connect-timeout 10 --max-time 10 http://51.210.128.134:5005/api/health)"
          
      - name: Send alert in telegram
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            Heartbeat 💗
            51.210.128.134:5000/api/health | ${{ steps.ping.outputs.status1 }}
            51.210.128.134:5005/api/health | ${{ steps.ping.outputs.status2 }}
